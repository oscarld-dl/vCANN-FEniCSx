# vCANN training environment (vcann/), Python 3.10.
#
# Reproduces the trained VHB4910 checkpoint under TensorFlow 2.10 / Keras 2, as
# used by the upstream vCANN implementation.
#
# This is NOT the environment for the FEniCSx bridge in fenicsx/, which needs
# Keras 3 — see requirements-bridge.txt. TensorFlow 2.10 and 2.21 cannot share
# an environment, so keep the two in separate virtual environments.

matplotlib==3.8.2
numpy==1.26.0
scipy==1.11.3
scikit-learn==1.3.0
keras==2.10.0
tensorflow==2.10.0
tqdm==4.65.0
kormos==0.1.4
plotly==5.9.0
natsort==8.4.0
